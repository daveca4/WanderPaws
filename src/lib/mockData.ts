import { Dog, Owner, Walker, Walk, Assessment } from './types';

// Empty mock data arrays
export const mockDogs: Dog[] = [];
export const mockOwners: Owner[] = [];
export const mockWalkers: Walker[] = [];
export const mockWalks: Walk[] = [];
export const mockAssessments: Assessment[] = [];

// Add any other empty arrays or utility functions needed by components 